export enum Entity {
  STUDENT = 'student',
  UNIVERSITY = 'university',
  COMPANY = 'company',
  MOD = 'mod', // of either company or uni
}

export enum MessageParticipantType {
  USER = 'User',
  COMPANY = 'Company',
  UNIVERSITY = 'University',
}
